# Docs Layout

Primary docs:
1. `agent-map.source.md` - canonical source for `CLAUDE.md` and `chatgpt.md`.
2. `adr/` - Architectural Decision Records.
3. `ARCHITECTURE.md` (repo root) - supporting architecture reference.

Automation:
1. `npm run docs:sync` - regenerate agent docs.
2. `npm run docs:verify-sync` - verify generated docs are in sync.

