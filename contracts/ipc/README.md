# IPC Payload Contracts

Files:
1. `payload_schemas.js` - request/response/event payload schemas keyed by `CHANNEL.*` and `EVENT.*`.

Validation:
1. `npm run ipc:contracts` - schema coverage + fixture sample validation.
2. `npm run ipc:check` - channel/event registration consistency.

